spring:
  application:
    name: service-ribbon
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        enabled: true
        serviceId: config-server
server:
  port: 8764